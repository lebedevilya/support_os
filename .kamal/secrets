KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
RAILS_MASTER_KEY=$(cat config/master.key)
# GITHUB_WEBHOOK_SECRET and OPENCLAW_HOOKS_TOKEN intentionally not here:
# they live in encrypted Rails credentials and are decrypted in-app with
# RAILS_MASTER_KEY, so they survive every deploy without a shell handshake.
